DATABASE_URL=
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
NEXT_PUBLIC_PAYPAL_CLIENT_ID=

# Handoff FeNAM -> Enotempo
# Secret per firmare token HMAC (genera una stringa casuale sicura, es. con: openssl rand -hex 32)
FENAM_HANDOFF_SECRET=
# Opzionale: host consentiti per redirect (separati da virgola, default: enotempo.it,www.enotempo.it)
# FENAM_ALLOWED_RETURN_HOSTS=enotempo.it,www.enotempo.it

# Opzionale: chiave segreta per proteggere endpoint dev/test (solo development)
# Se configurata, gli endpoint /api/dev/* richiedono header X-DEV-KEY con questo valore
# DEV_ONLY_KEY=
